# Payslip PDF rendering (internal route secret)
PAYSLIP_RENDER_SECRET=

# Codeable Ticketing backend (Express API) used by the Engineering tools
# (Generate Tickets, Velocity, Feature Access). Users authenticate to it
# with a personal access token pasted in the UI; this is just the base URL.
NEXT_PUBLIC_TICKETING_API_URL=http://localhost:3000
