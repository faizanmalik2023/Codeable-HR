# Shared secret for the internal payslip render endpoint.
# The backend must send this value in the x-render-secret header.
# Use a strong random value in production (e.g. openssl rand -hex 32).
PAYSLIP_RENDER_SECRET=dev-secret-change-me
NEXT_PUBLIC_TICKETING_API_URL=http://127.0.0.1:39412
